super + ctrl + shift + r
    pkill -USR1 -x sxhkd

XF86AudioMicMute
    pactl set-source-mute @DEFAULT_SOURCE@ toggle

XF86MonBrightnessDown
    ~/.config/sxhkd/scripts/volume-brightness brightness_down
XF86MonBrightnessUp
    ~/.config/sxhkd/scripts/volume-brightness brightness_up

XF86AudioLowerVolume
    ~/.config/sxhkd/scripts/volume-brightness volume_down
XF86AudioRaiseVolume
    ~/.config/sxhkd/scripts/volume-brightness volume_up
XF86AudioMute
    ~/.config/sxhkd/scripts/volume-brightness volume_mute

XF86AudioPlay
    playerctl -p 'spotify' play-pause
XF86AudioPause
    playerctl -p 'spotify' play-pause

ctrl + backslash
    playerctl -p 'spotify' play-pause
ctrl + shift + backslash
    playerctl --all-players pause
ctrl + shift + {braceleft,jcircumflex}
    playerctl -p 'spotify' previous
ctrl + shift + {braceright,hcircumflex}
    playerctl -p 'spotify' next

super + shift + p
    ~/scripts/cmd/bg-change

super + Return
    kitty tmux -L FOO
super + alt + Return
    kitty tmux -L FOO attach
super + controL + Return
    kitty sh -c 'printf "\033[1m(No tmux)\n" && zsh'

super + alt + Delete
    ~/scripts/cmd/kill-orphans

super + d
    rofi -show drun
super + shift + d
    rofi -show run
super + control + d
    rofi -show window
super + alt + d
    dmenu_run

super + period
    ~/scripts/cmd/emoji-menu

super + v
    clipcat-menu
super + alt + v
    clipcatctl clear

# bspc node --kill
super + {q, scircumflex}
    ~/.config/sxhkd/scripts/kill-window
super + control + {q, scircumflex}
    i3-msg kill

super + {h,j,k,l}
    i3-msg focus {left,down,up,right}

super + shift + {h,j,k,l}
    i3-msg move {left,down,up,right}

super + f
    i3-msg fullscreen toggle

# TODO: Doesn't need Esperanto versions ?
super + {w, gcircumflex}
    i3-msg layout tabbed
super + e
    i3-msg layout toggle split

super + shift + m
    ~/scripts/cmd/mount-gui --notify

super + b
    ~/scripts/cmd/bt connect
super + shift + b
    ~/scripts/cmd/bt disconnect
super + ctrl + b
    ~/scripts/cmd/bt reconnect

super + g
    ~/scripts/cmd/garf show

super + o
    kitty tmux -L FOO new 'cd ~/docs/notes && nvim $(notename) && zsh'

Print
    flameshot gui

super + Escape
    ~/scripts/cmd/powermenu

super + s
    ~/scripts/cmd/player-info 'spotify' 'notify'
super + shift + s
    dunstify -t 2000 "$(date '+%H:%M | %a %d %b')"

mod + ctrl + n
    dunstctl close-all

alt + F1
    ~/scripts/cmd/esperanto toggle
alt + shift + F1
    ~/scripts/cmd/esperanto off

# TODO: Broken!
# ctrl + ccircumflex
#     xdotool keyup 'ccircumflex'; xdotool key 'x'
# ctrl + gcircumflex
#     xdotool keyup 'gcircumflex'; xdotool key 'w'
# ctrl + ubreve
#     xdotool keyup 'ubreve'; xdotool key 'y'
#
# alt + scircumflex
#     ~/.config/sxhkd/scripts/alt-key 'scircumflex' 'q'
# alt + gcircumflex
#     ~/.config/sxhkd/scripts/alt-key 'gcircumflex' 'w'
# alt + ubreve
#     ~/.config/sxhkd/scripts/alt-key 'ubreve' 'y'
# alt + ccircumflex
#     ~/.config/sxhkd/scripts/alt-key 'ccircumflex' 'x'
# alt + jcircumflex
#     ~/.config/sxhkd/scripts/alt-key 'jcircumflex' 'bracketleft'
# alt + hcircumflex
#     ~/.config/sxhkd/scripts/alt-key 'hcircumflex' 'bracketright'

###################################################
# Everything below here is defaults for bspwm !!! #
###################################################

#
# bspwm hotkeys
#

# quit/restart bspwm
super + alt + {q,r}
    bspc {quit,wm -r}

# close and kill
super + {_,shift + }w
    bspc node -{c,k}

# alternate between the tiled and monocle layout
super + m
    bspc desktop -l next

# send the newest marked node to the newest preselected node
super + y
    bspc node newest.marked.local -n newest.!automatic.local

# swap the current node and the biggest window
super + g
    bspc node -s biggest.window

#
# state/flags
#

# set the window state
# super + {t,shift + t,s,f}
#     bspc node -t {tiled,pseudo_tiled,floating,fullscreen}

# set the node flags
super + ctrl + {m,x,y,z}
    bspc node -g {marked,locked,sticky,private}

#
# focus/swap
#

# focus the node in the given direction
# super + {_,shift + }{h,j,k,l}
#     bspc node -{f,s} {west,south,north,east}

# focus the node for the given path jump
super + {p,b,comma,period}
    bspc node -f @{parent,brother,first,second}

# focus the next/previous window in the current desktop
super + {_,shift + }c
    bspc node -f {next,prev}.local.!hidden.window

# focus the next/previous desktop in the current monitor
super + bracket{left,right}
    bspc desktop -f {prev,next}.local

# focus the last node/desktop
super + {grave,Tab}
    bspc {node,desktop} -f last

# focus the older or newer node in the focus history
super + {o,i}
    bspc wm -h off; \
    bspc node {older,newer} -f; \
    bspc wm -h on

# focus or send to the given desktop
super + {_,shift + }{1-9,0}
    bspc {desktop -f,node -d} '^{1-9,10}'

#
# preselect
#

# preselect the direction
super + ctrl + {h,j,k,l}
    bspc node -p {west,south,north,east}

# preselect the ratio
super + ctrl + {1-9}
    bspc node -o 0.{1-9}

# cancel the preselection for the focused node
super + ctrl + space
    bspc node -p cancel

# cancel the preselection for the focused desktop
super + ctrl + shift + space
    bspc query -N -d | xargs -I id -n 1 bspc node id -p cancel

#
# move/resize
#

# expand a window by moving one of its side outward
super + alt + {h,j,k,l}
    bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}

# contract a window by moving one of its side inward
super + alt + shift + {h,j,k,l}
    bspc node -z {right -20 0,top 0 20,bottom 0 -20,left 20 0}

# move a floating window
super + {Left,Down,Up,Right}
    bspc node -v {-20 0,0 20,0 -20,20 0}

