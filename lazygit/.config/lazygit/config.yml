gui:
  theme:
    selectedLineBgColor:
        - underline
    selectedRangeBgColor:
        - underline

git:
  paging:
    pager: delta --dark --paging=never

customCommands:
  - key: 'ŝ'
    context: 'global'
    command: 'kill "$PPID"'

os:
  editPreset: 'nvim'

