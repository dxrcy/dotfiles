gui:
  theme:
    selectedLineBgColor:
        - underline
    selectedRangeBgColor:
        - underline

git:
  paging:
    pager: delta --dark --paging=never

customCommands:
  - key: 'ŝ'
    context: 'global'
    command: 'kill "$PPID"'
  - key: 'G'
    context: 'global'
    command: 'gh auth switch'

os:
  editPreset: 'nvim'

