set from = "acednes@gmail.com"
set realname = "darcy"

# IMAP settings
set imap_user = "acednes@gmail.com"

# SMTP settings
set smtp_url = "smtps://acednes@smtp.gmail.com"

# Remote Gmail folders
set folder = "imaps://imap.gmail.com/"
set spoolfile = "+INBOX"
set postponed = "+[Gmail]/Drafts"
set record = "+[Gmail]/Sent Mail"
set trash = "+[Gmail]/Trash"
 
